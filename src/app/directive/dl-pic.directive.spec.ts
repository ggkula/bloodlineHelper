import { DlPicDirective } from './dl-pic.directive';

describe('DlPicDirective', () => {
  it('should create an instance', () => {
    const directive = new DlPicDirective();
    expect(directive).toBeTruthy();
  });
});
